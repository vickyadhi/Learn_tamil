import 'package:flutter/material.dart';
import 'package:learn_tamil/Start%20page.dart';
void main() {
  runApp(MaterialApp(home: Startpage()));
}

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Startpage(
    );
  }
}
